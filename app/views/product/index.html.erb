<div class="container">
  <% @products.each do |product| %>
    <div class="row well">
      <div class="col-xs-10">
        <%= link_to product_path(product) do %>
          <%= image_tag product.image.url %>
        <% end %>
      </div>
    </div>
  <% end %>
</div>
