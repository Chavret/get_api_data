<div class="container">
  <div class="row well margin-20">
    <% @products.each do |product| %>
      <div class="col-xs-4 margin-bottom-15">
        <%= link_to product_path(product) do %>
          <%= image_tag product.image.url %>
        <% end %>
      </div>
    <% end %>
  </div>
</div>
